<div class="@if(isset($class)){{ $class }}@endif flex items-center">
    <hr class="light-gray-divider flex-grow">
    @if(isset($title))
        <span class="mx-2">{{ $title }}</span>
    @endif
    <hr class="light-gray-divider flex-grow">
</div>
