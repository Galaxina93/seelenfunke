<script>
    // ==========================================
    // 1. OPT-IN SCREEN (LANDING PAGE)
    // ==========================================
    @include('livewire.customer.partials.scripts-partials.opt_in_screen')

    // ==========================================
    // 2. FUNKI HUB (DAS DASHBOARD NACH DEM OPT-IN)
    // ==========================================
    @include('livewire.customer.partials.scripts-partials.funki_hub')

    // ==========================================
    // 3. DEEP SPACE & WARP ENGINE (Haupt-Hintergrund)
    // ==========================================
    @include('livewire.customer.partials.scripts-partials.space_warp_engine')
</script>
